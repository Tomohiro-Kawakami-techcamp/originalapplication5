<h1>トップページ</h1>

<%= link_to "登録する", new_schedule_path, class: :nav__btn %>

<% @schedules.each do |schedule| %>
  <%= schedule.user.name %>
  <%= schedule.title %>
  <%= schedule.content %>
  <%= schedule.day %>
<% end %>